# AUTO-GENERATED by scripts/aggregate.mjs — do not edit
# This file is derived from the aggregate.yml manifest in this package;
# edit aggregate.yml and rerun `node scripts/aggregate.mjs`.
# Rows are namespaced web-ui-* so the bundle can coexist with standalone installs.

# from self
- insert:
    - id: web-ui-compat
      name: '@dsh-selfuse/web-ui-all'

# from ../dsh-web-ui-settings
- insert:
    - id: web-ui-settings
      name: '@dsh-selfuse/web-ui-settings'

# from ../dsh-community-plugins
- insert:
    - id: web-ui-community-plugins
      name: '@dsh-selfuse/web-ui-community-plugins'

# from ../dsh-task-board
- insert:
    - id: web-ui-task-board
      name: '@dsh-selfuse/web-ui-task-board'

# from ../dsh-git-graph
- insert:
    - id: web-ui-git-graph
      name: '@dsh-selfuse/web-ui-git-graph'

# from ../dsh-remote-web-ui
- insert:
    - id: web-ui-remote-web-ui
      name: '@dsh-selfuse/remote-web-ui'

# from ../dsh-ssh
- insert:
    - id: web-ui-ssh
      name: '@dsh-selfuse/ssh'

# from ../dsh-chat-recovery
- insert:
    - id: web-ui-chat-recovery
      name: '@dsh-selfuse/chat-recovery'

# from ../skins/skin-center (via ../dsh-skins)
- insert:
    - id: web-ui-skin-center
      name: '@dsh-selfuse/skin-center'

# external: @dsh-selfuse/better-sidebar
- insert:
    - id: web-ui-better-sidebar
      name: '@dsh-selfuse/better-sidebar'

# host apiproxy SDK compatibility
- insert:
    - id: web-ui-host-apiproxy
      name: '@deepseek-ai/dsh-host-apiproxy'
